# 🚀 دليل رفع التعديلات على AWS Server

## ✅ الطريقة الأولى: النشر التلقائي عبر GitHub (موصى بها)

المشروع مجهز بنظام **CI/CD تلقائي** يقوم برفع التعديلات تلقائياً عند عمل Push!

### الخطوات:

#### 1️⃣ حفظ التعديلات في Git
```bash
# انتقل لمجلد المشروع
cd "d:\boody\CRM\السي ار ام الي شغال\crm netaq last update\netaq-crm-clean-20251001-103636"

# تأكد من حالة Git
git status

# أضف جميع التعديلات
git add .

# احفظ التعديلات مع رسالة واضحة
git commit -m "Add user edit feature - تعديل بيانات المستخدمين"
```

#### 2️⃣ ارفع على GitHub
```bash
# ارفع على الفرع الرئيسي (main أو master)
git push origin main

# أو إذا كان الفرع اسمه master
git push origin master
```

#### 3️⃣ انتظر النشر التلقائي
- بمجرد عمل `git push`، سيبدأ **GitHub Actions** تلقائياً
- يمكنك متابعة التقدم من:
  - GitHub → المستودع → تبويب **Actions**
- سيقوم النظام بـ:
  - ✅ تثبيت التبعيات
  - ✅ بناء Frontend
  - ✅ إنشاء نسخة احتياطية
  - ✅ نشر الكود الجديد على AWS
  - ✅ إعادة تشغيل الخدمات
  - ✅ فحص صحة النظام

#### 4️⃣ تأكد من النشر
بعد 5-10 دقائق، افتح الموقع:
```
https://www.netaqcrm.site
```

---

## 🔧 الطريقة الثانية: النشر اليدوي (للطوارئ)

إذا كنت تريد رفع التعديلات مباشرة بدون GitHub:

### الخطوات:

#### 1️⃣ اتصل بالسيرفر
```bash
# استخدم SSH للاتصال بـ AWS EC2
ssh -i "your-key.pem" ubuntu@your-ec2-ip

# أو إذا كان لديك الـ IP في متغير
ssh ubuntu@$(cat EC2_HOST)
```

#### 2️⃣ انتقل لمجلد المشروع
```bash
cd ~/crm
# أو
cd /home/ubuntu/crm
```

#### 3️⃣ اسحب آخر التحديثات
```bash
# اسحب من GitHub
git pull origin main

# أو
git pull origin master
```

#### 4️⃣ ثبت التبعيات (إذا لزم الأمر)
```bash
# Frontend
cd crm-frontend-only
npm install
npm run build

# Backend (إذا كان هناك تغييرات)
cd ../crm-backend
npm install
```

#### 5️⃣ أعد تشغيل الخدمات
```bash
# أعد تشغيل Backend
pm2 restart crm-backend

# أعد تحميل Nginx
sudo systemctl reload nginx

# تحقق من الحالة
pm2 status
```

---

## 📋 الطريقة الثالثة: رفع ملف مباشر (للتعديلات الصغيرة)

إذا كنت تريد رفع ملف واحد فقط:

#### 1️⃣ استخدم SCP لنقل الملف
```bash
# من جهازك المحلي
scp -i "your-key.pem" "d:\boody\CRM\السي ار ام الي شغال\crm netaq last update\netaq-crm-clean-20251001-103636\crm-frontend-only\src\pages\UserManagement.jsx" ubuntu@your-ec2-ip:~/crm/crm-frontend-only/src/pages/

# ثم اتصل بالسيرفر وأعد البناء
ssh -i "your-key.pem" ubuntu@your-ec2-ip
cd ~/crm/crm-frontend-only
npm run build
sudo systemctl reload nginx
```

---

## 🎯 الأوامر السريعة (Quick Commands)

### للنشر التلقائي:
```bash
cd "d:\boody\CRM\السي ار ام الي شغال\crm netaq last update\netaq-crm-clean-20251001-103636"
git add .
git commit -m "Update: User edit feature"
git push origin main
```

### للتحقق من حالة النشر:
```bash
# على GitHub
# اذهب إلى: https://github.com/boodymasoud7/netaq-crm/actions

# على السيرفر
ssh ubuntu@your-server-ip
pm2 status
pm2 logs crm-backend --lines 50
```

### لإعادة تشغيل الخدمات على السيرفر:
```bash
ssh ubuntu@your-server-ip
pm2 restart crm-backend
sudo systemctl reload nginx
```

---

## ⚠️ ملاحظات مهمة

### قبل الرفع:
- ✅ تأكد من أن التعديلات تعمل محلياً
- ✅ احفظ جميع الملفات
- ✅ تأكد من عدم وجود أخطاء في الكود

### بعد الرفع:
- ✅ تحقق من الموقع: https://www.netaqcrm.site
- ✅ جرّب تسجيل الدخول
- ✅ جرّب ميزة تعديل المستخدمين
- ✅ تحقق من عدم وجود أخطاء في Console

### في حالة المشاكل:
```bash
# تحقق من logs
ssh ubuntu@your-server-ip
pm2 logs crm-backend --lines 100
sudo tail -f /var/log/nginx/error.log

# أعد تشغيل كل شيء
pm2 restart all
sudo systemctl restart nginx
```

---

## 🔍 التحقق من نجاح النشر

### 1. تحقق من GitHub Actions
- اذهب إلى: `https://github.com/boodymasoud7/netaq-crm/actions`
- تأكد من أن آخر Workflow نجح (✅ علامة خضراء)

### 2. تحقق من الموقع
```
https://www.netaqcrm.site
```
- سجل دخول
- اذهب إلى "إدارة المستخدمين"
- جرّب زر "تعديل" على أي مستخدم
- تأكد من ظهور النافذة المنبثقة
- جرّب تعديل اسم مستخدم
- احفظ وتأكد من التحديث

### 3. تحقق من Backend
```bash
ssh ubuntu@your-server-ip
pm2 status
# يجب أن يكون crm-backend في حالة "online"
```

---

## 📊 الجدول الزمني المتوقع

| الخطوة | الوقت المتوقع |
|--------|---------------|
| Git Push | فوري |
| GitHub Actions يبدأ | 10-30 ثانية |
| بناء المشروع | 2-3 دقائق |
| النشر على AWS | 1-2 دقيقة |
| إعادة تشغيل الخدمات | 10-20 ثانية |
| **الإجمالي** | **5-7 دقائق** |

---

## 🆘 حل المشاكل الشائعة

### المشكلة: Git Push فشل
```bash
# تحقق من الاتصال
git remote -v

# تحقق من الفرع
git branch

# حاول مرة أخرى
git push origin main --force
```

### المشكلة: GitHub Actions فشل
- تحقق من logs في GitHub Actions
- تأكد من صحة AWS Secrets في GitHub
- تحقق من اتصال GitHub بـ AWS

### المشكلة: الموقع لا يعمل بعد النشر
```bash
# اتصل بالسيرفر
ssh ubuntu@your-server-ip

# تحقق من الخدمات
pm2 status
sudo systemctl status nginx

# أعد التشغيل
pm2 restart all
sudo systemctl restart nginx

# تحقق من الأخطاء
pm2 logs crm-backend --lines 50
```

---

## 🎉 بعد النشر الناجح

✅ التعديلات الآن مباشرة على:
- **الموقع**: https://www.netaqcrm.site
- **الميزة الجديدة**: تعديل بيانات المستخدمين
- **متاح لجميع المستخدمين**: فوراً

---

## 📞 للدعم

إذا واجهت أي مشكلة:
1. تحقق من GitHub Actions logs
2. تحقق من PM2 logs على السيرفر
3. تحقق من Nginx logs
4. راجع هذا الدليل

---

**آخر تحديث**: 30 نوفمبر 2025
**الحالة**: ✅ جاهز للنشر
